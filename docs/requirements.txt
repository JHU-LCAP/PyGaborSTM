# Documentation tooling only. The package and its runtime/plotting
# dependencies are installed from pyproject.toml via .readthedocs.yaml,
# so nothing here duplicates a dependency list.

mkdocs>=1.6,<2.0
mkdocs-material>=9.5,<10.0
mkdocstrings[python]>=1.0,<2.0
